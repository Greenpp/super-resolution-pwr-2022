program: main.py
name: scale x2
method: grid
parameters:
  train-class:
    values: ["dog", "cat", "mix"]
  random-seed:
    values: [42, 100, 300, 253, 82294]
command:
  - ${env}
  - python
  - ${program}
  - ${args}
